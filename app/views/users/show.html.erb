<div class="jumbotron text-black">
	<div class="jumbotron col-md-6">

<p id="notice" class="h5"><%= notice %></p>

<p>
  <strong>メールアドレス:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>氏名:</strong>
  <%= @user.first_name %>  <%= @user.last_name %>
</p>

<p>
   <td><%= image_tag(@user.image_path_url) rescue nil %></td>
</p>

<p>
  <strong>アレルギー品目</strong>
    <% @user.allergens.each do |allergen| %>
  	 	<%= allergen.name %> &nbsp
  	<% end %>
</p>
 	<%= link_to "トップページへ", root_path, method: :get, class: "btn btn-color text-light" %>
	</div>
</div>
