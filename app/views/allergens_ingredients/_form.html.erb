<%= form_with(model: allergens_ingredient, local: true) do |form| %>
  <% if allergens_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(allergens_ingredient.errors.count, "error") %> prohibited this allergens_ingredient from being saved:</h2>

      <ul>
      <% allergens_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.text_field :ingredient_id %>
  </div>

  <div class="field">
    <%= form.label :allergen_id %>
    <%= form.text_field :allergen_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
